# When the static site is served from a non-Cloudflare host (e.g. Hostinger),
# all /api/* fetches must point to the deployed Worker URL.
# Leave blank when deploying to Cloudflare itself.
VITE_API_URL=https://yakshagana-app.prasadcherkady.workers.dev
